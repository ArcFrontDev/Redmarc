# Translation Locales

This directory is reserved for i18n translation files (e.g., `en.json`, `ru.json`).
Elena (`loc-elena`) will begin configuring the translation system and mapping keys here in the next sprint.
